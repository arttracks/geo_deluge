require "spec_helper"

describe GeoDeluge do
  it "has a version number" do
    expect(GeoDeluge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
